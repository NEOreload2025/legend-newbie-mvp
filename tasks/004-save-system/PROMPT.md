請完整實作 `tasks/004-save-system/SPEC.md` 描述的存檔系統。

工作要求：
1. 先閱讀 `AGENTS.md`、`tasks/004-save-system/SPEC.md`，再閱讀 `src/data/GameState.ts`、`src/scenes/ClassSelectScene.ts`、`src/scenes/VillageScene.ts`、`src/entities/Player.ts`
2. 依 SPEC 實作
3. 最後在專案根目錄執行 `npx tsc` 與 `npm run build`（不要對單一檔案跑 tsc），必須零錯誤
4. 禁止：修改 `tasks/`、修改 `src/systems/` 既有檔案（新增 SaveSystem.ts 除外）、啟動 dev server、安裝新依賴
5. 完成後條列摘要：改了哪些檔案、做了哪些 SPEC 未明說的決策
